import { CarouselPosition } from '../../../types/carousel.ts';
import { Gradient } from '../../../types/gradient.ts';
export declare const getEffectiveBackgroundColorFromGradient: ({ carouselPosition, gradient, }: {
    carouselPosition: CarouselPosition;
    gradient: Gradient;
}) => string | null;
//# sourceMappingURL=getEffectiveBackgroundColorFromGradient.d.ts.map