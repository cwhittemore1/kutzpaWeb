import { JSX } from 'preact';
import type { Players } from '../../../types/player-api-types.ts';
interface PreloadThumbnailProps {
    isPlayPending: boolean;
    mediaId: string;
    playerType: Players;
    playerWidth: number;
}
export declare const PreloadThumbnail: ({ isPlayPending, mediaId, playerType, playerWidth, }: PreloadThumbnailProps) => JSX.Element;
export {};
//# sourceMappingURL=PreloadThumbnail.d.ts.map